<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.usuario.sqlandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".createCategoria" android:label="Categorias"></activity>
        <activity android:name=".CategoriasList" android:label="Todas las Categorias"></activity>
        <activity android:name=".ListProductos" android:label="Todas los Productos"></activity>
        <activity android:name=".MainCategorias" android:label="Index Categorias"></activity>
        <activity android:name=".MainProductos" android:label="Index Productos"></activity>
        <activity android:name=".MainCompras" android:label="Index Compras"></activity>
        <activity android:name=".ListCompras" android:label="Mis Compras"></activity>
    </application>
</manifest>